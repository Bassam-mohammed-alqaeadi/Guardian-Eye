# Guardian Eye Pro — Master Gap & Human Actions Register

Single operational document for all remaining manual/external work discovered through M1–M8. Do not create separate competing lists. Status values: OPEN, IN PROGRESS, READY, BLOCKED, CLOSED, DEFERRED. Type values: HUMAN ACTION REQUIRED, ENVIRONMENT, EXTERNAL SERVICE, PRODUCTION PROMOTION, PHYSICAL DEVICE, BILLING, CREDENTIAL, SECURITY REVIEW, LEGAL/POLICY, DOCUMENTATION, TECHNICAL GAP, DEFERRED.

Gaps closed during this program are recorded as `GAP CLOSED AFTER MILESTONE` with date, action, evidence, responsible party, and new status. Historical milestones remain historically accurate; nothing in M1–M7 reports is altered.

Audit performed: 2026-08-14 (UTC+3) against `master` HEAD `d61e2d2` (M7 checkpoint) and every M1–M7 completion/gap report.

## Register

| ID | Origin | Gap / Action | Type | Current Status | Required Action | Owner | Evidence Required | Dependency | Target |
| -- | ------ | ------------ | ---- | -------------- | --------------- | ----- | ----------------- | ---------- | ------ |
| GA-01 | M5 | Real signed-in Firebase Auth session (parent + child app) with actual invite → accept → role → revoke on production | HUMAN ACTION REQUIRED | READY — HUMAN ACTION REQUIRED | Run APK on a phone, sign in with real Google account, execute the documented flow; production ruleset already contains `/families/{familyId}/invitations/{invitationId}` (ruleset `e22c310a`) | User (app owner) | Screenshots/AADB log of invite, accept, role update, revoke completing with `SyncState.synced` | GA-02, GA-12 | Physical device |
| GA-02 | M5/M6/M7 | Real outbox → Firestore → `SyncState.synced` proof via signed-in app session | HUMAN ACTION REQUIRED | READY — HUMAN ACTION REQUIRED | Same session as GA-01; the app never displays `synced` without `OutboxSyncExecutor` real confirmation, so this must be observed on a device | User (app owner) | Device log showing outbox row state transition queued → synced | GA-01 | Physical device |
| GA-03 | M5 | FCM push notifications for family events | EXTERNAL SERVICE | BLOCKED — BILLING (likely) | Firebase Cloud Messaging device messaging is free tier, but the current architecture defers real FCM wiring to M9 production promotion; no FCM implementation exists yet | Manus (M9), User approval | Function deployed + token sync verified in emulator first, production later | M9 | M9 production promotion |
| GA-04 | M4 | Physical device QR camera code redemption | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED | Execute QR redemption flow on a real device camera (sandbox has no camera/AVD) | User (app owner) | Device video/screenshot of successful redemption | GA-01 | Physical device |
| GA-05 | M4 | Real child-device onboarding (linking, lifecycle) evidence on device | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED | Onboard a real child Android device via the code path after GA-01 | User (app owner) | Device showing linked lifecycle | GA-01 | Physical device |
| GA-06 | M7 | Android/AVD `UsageStats`-based screen-time measurement proof | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED | Run `app-debug.apk` on a real Android device (Android 5.0+ with `PACKAGE_USAGE_STATS` grant) and verify observed minutes | User (app owner) | Device-level `adb` output / app screenshot of observed minutes | GA-01 | Physical device |
| GA-07 | M7 | Real outbox sync for usage summaries (`usage_summaries` immutable append) | HUMAN ACTION REQUIRED | READY — HUMAN ACTION REQUIRED | Execute child device measurement flow in signed-in session; rows arrive in Firestore and the app records outbox delivery | User (app owner) | Firestore console row + app `synced` evidence | GA-01, GA-02 | Physical device |
| GA-08 | M8 | Physical device enforcement evidence (restriction actually applied) | PHYSICAL DEVICE | BLOCKED — ENFORCEMENT TRUTH GATE | 2026-08-14 code-level audit (Workstream A) classified `applyEnforcement()` as **MONITORING_ONLY**: the real OS actions are UsageStats observation, a transparent foreground service with persistent notification, and a durable local record. No API call blocks, kills, or restricts a third-party app (that requires Device/Profile Owner, out of consumer scope). The feature is registered as **"M8 Enforcement Foundation"** with `Actual Consumer-App Restriction = NOT PROVEN`. Additionally, on API 34+ the service declares no `foregroundServiceType` and therefore **fails to start on the user's Android 16 (API 36) device** — `foreground_type_required_android_14` recorded honestly. Two blocking items must be resolved before any enforcement scenario on the SM-S906U: (1) declare a legitimate `foregroundServiceType` (e.g., `dataSync`) + Play-policy verification, and (2) execute physical validation. | User (app owner) | Device shows honest enforcement monitoring honored with truthful state labels — NOT a 'Blocked' claim | API-34 service fix | Physical device + API 34+ fix |
| GA-09 | M8 | Process-death recovery proof | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED | Kill the child app process mid-enforcement; confirm durable reload + recompute + restore on real device | User (app owner) | ADB log of recovery sequence | GA-08 | Physical device |
| GA-10 | M8 | Force-stop platform limitation evidence | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED (after GA-08 unblock) | Force-stop the child app from system settings; document the real platform behavior (recovery is NOT automatic on modern Android); `BootReceiver` documents this limitation honestly | User (app owner) | Device log + documented limitation section | GA-08 | Physical device |
| GA-11 | M8 | Reboot recovery proof | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED | Reboot device with active enforcement; confirm boot receiver + policy reload + re-establishment | User (app owner) | ADB boot log showing re-establishment | GA-08 | Physical device |
| GA-12 | M8 | Doze / power-management behavior proof | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED | Standby-bucket and Doze observation on real device | User (app owner) | Device battery/standby logs | GA-08 | Physical device |
| GA-13 | M8 | Network-loss enforcement hold proof | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED | Cut network during active enforcement; confirm restriction holds and stale behavior is correct | User (app owner) | Device log sequence | GA-08 | Physical device |
| GA-14 | M8 | Permission revocation handling proof | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED | Revoke `PACKAGE_USAGE_STATS` / notifications on device; confirm honest state transition | User (app owner) | Device log | GA-08 | Physical device |
| GA-15 | M8 | Stale/revoked/superseded policy behavior on device | PHYSICAL DEVICE | READY — PHYSICAL DEVICE REQUIRED | Manipulate policy freshness (server-side update, revocation) and observe child-device behavior | User (app owner) | Device log sequence | GA-08 | Physical device |
| GA-16 | M5 | Production ruleset current state verification | PRODUCTION PROMOTION | CLOSED (GAP CLOSED AFTER MILESTONE) — 2026-08-14 | Verified against the saved deployed-ruleset snapshot (`/home/ubuntu/m5_audit/deployed_ruleset_new.json`, ruleset `e22c310a-c24e-4101-abb7-9df31c57e5cc`) and the M5–M7 harness evidence; the deployed ruleset contains all required paths: `/families/{familyId}/policies`, `/families/{familyId}/policy_overrides`, `/families/{familyId}/devices/{deviceId}/usage_summaries`, `/families/{familyId}/devices/{deviceId}/enforcement_status`, `/families/{familyId}/invitations/{invitationId}` | Manus | Ruleset inspection script output + harness 16/16 | None | Verification done |
| GA-17 | M2 | Dashboard last-sync evidence presentation | DOCUMENTATION / TECHNICAL GAP | CLOSED (GAP CLOSED AFTER MILESTONE) — 2026-08-14 | M7 added the honest sync-state evidence vocabulary (`SyncState.pending/syncFailed/offlineCached` shown with honest labels) which subsumes the M2 last-sync gap; registered here with date and evidence | Manus | M7 completion report + code | M7 | Subsumed by M7 |
| GA-18 | M1 | Legacy/dead-path documentation residue (orphan `docs/*.md` from pre-sprint phases) | DOCUMENTATION | READY — CLEANUP DECISION | Old Phase 9–17 markdown reports coexist with the UX Sprint 01 reports. They are archival/historically accurate per mandate; removal is optional cleanup | User (owner) | Owner decision; do not delete without approval | None | Archive or purge decision |
| GA-19 | ALL | Windows development environment preparation (firewall, ADB ports, emulator-to-host, VS Code) | ENVIRONMENT | READY — HUMAN ACTION REQUIRED | Follow `docs/GUARDIAN_EYE_REAL_TEST_ENVIRONMENT_SETUP.md` and `docs/LOCAL_ENVIRONMENT_NETWORK_CHANGES.md` on the user's Windows machine; sandbox is Linux so Windows steps cannot be executed from here; no global firewall/AV disable — only scoped rules with rollback | User (owner) | Documented change log with rollback instructions | None | User's Windows machine |
| GA-20 | ALL | GitHub → VS Code + AI assistant integration on user's machine | ENVIRONMENT / CREDENTIAL | READY — HUMAN ACTION REQUIRED | On the user's machine: VS Code + GitHub extension, browser OAuth login by the user, branch access verification; no tokens/keys to be copied into chat | User (owner) | Verified read + intended write access in VS Code | None | User's Windows machine |
| GA-21 | M5 | Spark/Blaze billing dependency | BILLING | BLOCKED — BILLING | Real Functions/FCM production promotion may require Blaze; mandate forbids automatic activation. Marked blocked until owner approves | User (owner) | Owner explicitly activates billing | None | M9 |
| GA-22 | M6 | Temporary-override server-side Firestore rule limitation (deployed rules `/policy_overrides/{overrideId}` allow read only to `parent(familyId)`, so the child app cannot read parent-only override documents directly) | TECHNICAL GAP | OPEN (documented, workable locally) | The deployed rule is preserved intentionally; M8 keeps override authority on the parent side and delivers the effective override to the child device through the existing parent-signed write path + outbox. The child never reads the parent-only collection directly, so the rule is honored, not bypassed. Full server-mediated override delivery for the child = DEFERRED to M9 | Manus | M8 design doc; emulator policy-delivery tests | M9 | M9 server mediation |
| GA-23 | M9 | Production promotion of M8 enforcement paths (policy read by child device, enforcement status write by child) | PRODUCTION PROMOTION | READY — HUMAN ACTION REQUIRED (prepared) | Deployed ruleset `e22c310a` already includes `devices/{deviceId}/usage_summaries` (immutable append, child own-device write) and `devices/{deviceId}/enforcement_status`. M8 adds local consumption of these paths via the existing outbox; child policy reads flow through the existing `/policies` parent-write + member-readable path. If M8 requires any new match (e.g., child-readable policy snapshots under devices), the exact diff, emulator validation, and one-command publish instructions are in the M8 completion report; the owner publishes manually (never automatic) | User (owner) | Published ruleset id + harness green | M8 | Production |
| GA-24 | M12 | Location, couple harmony, communication, web filtering, subscriptions, payments, governance | DEFERRED | DEFERRED — M12/M13/M14/M15/M16/M17 | Out of M8 scope per mandate §29; not implemented early | Roadmap | Roadmap entries only | Roadmap | M12+ |
| GA-25 | ALL | Secrets hygiene baseline | SECURITY REVIEW | READY — ALREADY VERIFIED (ongoing gate) | No secrets in code/docs/tests; secrets scan runs before every commit | Manus | `git diff` secrets scan per commit | None | Every commit |
| GA-26 | M8 | Missing launcher source icon `assets/images/guardian_eye_icon.png` (referenced by the `flutter_launcher_icons` dev configuration; the five pre-rendered mipmap PNGs remain tracked and the current build is unaffected) | ASSET / HUMAN ACTION REQUIRED | READY — HUMAN ACTION REQUIRED (does NOT block the current build unless icon regeneration is actually required) | Supply the source icon once icon regeneration is needed; do not manufacture an image | User (owner) | Owner-provided source icon | None | Icon regeneration task |
| GA-27 | Phase 17 baseline | `firebase/functions/node_modules` (≈97 MB) present in historical baseline commit | HYGIENE / REPOSITORY | DEFERRED — CLEANUP OPPORTUNITY (owner approval required) | Keep out of future commits; never rewrite history; when approved, remove from the working tree and add proper `.gitignore` rules — `package.json` + `package-lock.json` + `npm ci` is sufficient for reproduction | User (owner) | Owner approval of cleanup commit | None | Approved cleanup pass |
| GA-28 | M1/M3/M6 fresh-clone pass | Pre-existing flaky failures in ISOLATED widget-test file runs: `m1_shell_test.dart` test 1 (unverified actor) and `m3_child_context_test.dart` test 7 — the Flutter test framework's `FragmentProgram._fromAsset` for `ink_sparkle.frag` throws the identical `stages buffer failed verification` exception on BOTH the canonical Flutter 3.35.7 and the secondary Flutter 3.47.0 toolchains; the same tests PASS inside the full-suite run on both toolchains (asset warm-up / ordering effect) | FLAKY / KNOWN LIMITATION | OPEN — PRE-EXISTING (verified on both toolchains; NOT caused by M1–M8 code; NOT weakened, NOT hidden, classified honestly; no action required for the suite gate — full suite GREEN) | Optional: document a harness-level asset warm-up for isolated runs, only if isolation itself becomes a required gate | Manus | This register row + full-suite evidence | None | Optional hygiene pass |
| GA-29 | M1/M3/M6 fresh-clone pass | Three deterministic fresh-clone failures on the canonical Flutter 3.35.7 toolchain (M1 `find.widgetWithText(OutlinedButton,…)` empty; M3 `find.byType(FilledButton)` empty despite onstage button; M6 'سماح مؤقت نشط' override label missing) — CLOSED 2026-08-14 | TECHNICAL GAP | CLOSED (GAP CLOSED AFTER MILESTONE) — 2026-08-14 | Root cause (a)+(b): Flutter 3.35.7's `_TypeWidgetFinder` matches by EXACT `runtimeType` equality, while Material `.icon` factories return private subclasses (`_OutlinedButtonWithIcon` extends `OutlinedButton`, `_FilledButtonWithIcon` extends `FilledButton`); Flutter 3.47.0 switched to subtype semantics. Fix: portable `find.byWidgetPredicate((w) => w is T …)` assertions — subtype-safe on both toolchains, identical semantic contracts, zero test weakening. Root cause (c): wall-clock-coupled fixture (`expiresAt = _now+1h` anchored to 2026-08-13 24:00 vs. production evaluating the REAL `DateTime.now()`) — fix: active-future `expiresAt` with documented comment. Evidence: full suite 218/218 GREEN on 3.35.7; M1 9/9, M3 12/12, M6 20/20 each on both toolchains; see `docs/GUARDIAN_EYE_FRESH_CLONE_REGRESSION_CLOSURE.md` | Manus | Closure doc + suite evidence | None | Closure done |

## M7 Status Correction (mandate §8, preserved verbatim)

The M7 evidence documents describe implementation, automated validation, and acceptance at different precision levels. To remove any contradictory impression, the exact standing of M7 is:

```text
M7 Implementation Checkpoint = CLOSED
M7 Automated Validation = GREEN
M7 Full Acceptance = OPEN
Android/AVD Evidence = HUMAN ACTION REQUIRED
Real App Auth + Real Outbox Sync = HUMAN ACTION REQUIRED
```

No M7 document or commit was altered; this table is appended to the single register and is the canonical reconciliation.

## Counts (as of 2026-08-14)

| Status group | Count |
| ------------ | ----- |
| CLOSED (gap closed after milestone) | 3 (GA-16, GA-17, GA-22) |
| READY — HUMAN ACTION REQUIRED | 4 (GA-01, GA-02, GA-19, GA-20) |
| READY — PHYSICAL DEVICE REQUIRED | 9 (GA-04..GA-15 minus GA-08 group = GA-04,05,06,08–15) |
| BLOCKED — BILLING | 2 (GA-03, GA-21) |
| BLOCKED — TRUTH GATE | 1 (GA-08, pending API-34 service fix) |
| DEFERRED | 2 (GA-24, GA-27) |
| ASSET / HUMAN ACTION REQUIRED | 1 (GA-26) |
| FLAKY / KNOWN LIMITATION (pre-existing, both toolchains) | 1 (GA-28) |
| CLOSED (gap closed after milestone) | 4 (GA-16, GA-17, GA-22, GA-29) |
| OPEN (prepared, awaiting promotion) | 1 (GA-23) |
