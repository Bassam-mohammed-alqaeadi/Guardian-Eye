# Implementation Blockers — Guardian Eye Pro

**Flutter is the canonical mobile stack.** This project must not be converted to Expo or React Native. The active implementation workspace is `/home/ubuntu/guardian_eye_flutter` and is based on the available Guardian AI Flutter source snapshot.

| Blocked operation | Why blocked | Required command or action | Expected result | Completed without it | Verification remaining |
|---|---|---|---|---|---|
| Android APK release build in this sandbox | Android SDK 36, Build Tools 36.0.0, platform-tools, NDK 28.2.13676358، CMake 3.22.1، وJDK 17 أصبحت متاحة. أصلحت المراحل السابقة registrant/desugaring، وأضيف Crashlytics Gradle plugin 3.0.7 بعدما كشف build post-FlutterFire غيابه؛ بعد الإصلاح ما زال Gradle daemon يختفي تحت حد ذاكرة sandbox. | على عامل Android بذاكرة أكبر: `flutter clean && flutter pub get && GRADLE_OPTS='-Dorg.gradle.daemon=false -Dorg.gradle.jvmargs=-Xmx2048m -Dorg.gradle.workers.max=1' flutter build apk --release --target-platform android-arm64`. | ملف `build/app/outputs/flutter-apk/app-release.apk` مع hash وحجم و`aapt dump badging`. | `flutter analyze` بلا ملاحظات، 27 اختبار Flutter ناجح، FlutterFire Android config وCrashlytics plugin صحيحان محليًا. | APK حقيقي، ثم AVD/جهاز وتثبيت وتشغيل؛ لا يوجد APK أو دليل runtime حاليًا. |
| Android physical-device validation | `adb` متاح، لكن `adb devices -l` فارغ ولا Android Emulator/system image مثبت. | أوصل هاتف Android مع USB debugging، أو ثبّت AVD/system image على عامل مناسب؛ ثم `adb devices -l` و`adb install -r <APK>`. | يظهر serial/model/API/ABI ويثبت APK ويبدأ. | SDK/ADB/JDK صحيحة؛ لا جهاز افتراضي أو مادي متصل. | App launch، permissions، offline/recovery، Doze، FCM، وطرق accessibility الشرعية على جهاز فعلي. |
| iPhone build and simulator testing | This is a Linux host; Xcode, CocoaPods, and macOS are unavailable. Apple tooling is required to build/sign iOS apps. | Use macOS with current Xcode, CocoaPods, a Flutter SDK, and an Apple Developer team; run `flutter build ios --debug --no-codesign` or archive with valid signing. | iOS runner dependencies and simulator/device builds can execute. | Cross-platform Flutter layers, iOS entitlement templates, privacy strings, and documentation. | Validate iOS permission dialogs, Firebase setup, notifications, secure storage, and a signed device build. |
| Cloud Functions Guardian deployment | Firebase CLI is authorized and `manus-guardian` was verified; FlutterFire options were generated, Firestore rules/indexes deployed, and real Auth/Firestore tests passed. Firebase stopped `functions:guardian` before deployment because Artifact Registry/Cloud Build require the Blaze plan. | Owner must explicitly upgrade `manus-guardian` to Blaze in Firebase Console, then rerun `firebase deploy --only functions:guardian --project manus-guardian`. | `firebase functions:list --json` contains only the expected Guardian functions; logs show provisioning/notification execution under test. | TypeScript build, Emulator function tests, and real Auth/Firestore evidence. | Real callable provisioning, event-triggered notification processing, FCM backend acceptance. |
| Flutter client against Firebase Emulator | Auth وFirestore وFunctions Emulator شُغلت بنجاح من اختبارات Node، وFlutterFire Android options أصبحت مولدة رسميًا؛ لكن لا APK/AVD/جهاز لتشغيل عميل Flutter نفسه. | شغّل التطبيق مع `--dart-define=GUARDIAN_FIREBASE_CONFIGURED=true --dart-define=GUARDIAN_FIREBASE_USE_EMULATORS=true --dart-define=GUARDIAN_FIREBASE_EMULATOR_HOST=<HOST_REACHABLE_FROM_DEVICE>` على AVD أو جهاز. | تسجيل الدخول، إنشاء أسرة، Outbox، read-back، ورفض غير المصرح به من تطبيق Flutter إلى Emulator. | Auth callable provisioning وقواعد Firestore وincident/SOS notification events متحققة في Emulator من اختبارات معزولة. | دليل تطبيق Flutter ↔ Emulator وoffline/recovery على runtime حقيقي؛ لا يحتاج ذلك مشروع Firebase إنتاجيًا. |
| FCM/APNs physical delivery | Firebase project and Android app config now exist, but Guardian notification Function is not deployed (Blaze required), no real device token exists, and no physical Android/iPhone is connected. | Upgrade Blaze for `functions:guardian`, deploy it, register a real device token, then test foreground/background delivery on Android/iPhone. | Token registration, request, backend acceptance, delivery, and acknowledgement can be measured as separate states. | Local notification event, token outbox contract, guarded FCM service, and client-side acknowledgement model. | Physical delivery and acknowledgement evidence; no delivery state is currently claimed. |
| Child-device Firebase identity binding on real backend | Cloud Functions provisioning contract now creates a six-digit, SHA-256-hashed, 10-minute, one-use session and binds a distinct child UID atomically in Emulator; it is not deployed. | Deploy `functions:guardian`, enable Auth, then exercise create/redeem/replay rejection against the selected Firebase project. | Only a separately authenticated child UID is bound to the intended child and device; client-side UID injection remains denied. | Emulator evidence covers parent authorization, distinct child UID, one-time redemption, and replay rejection. | Real deployment evidence, Flutter callable-client wiring, and physical child-device proof. |
| On-device safety model inference | No reviewed TensorFlow Lite model artifact, labels, or model card is available. | Provide an approved model artifact with version, labels, licensing, threshold guidance, and test corpus. | Local model adapter can load and score supported inputs. | Provider-neutral pipeline, risk policy, incident persistence, and model-artifact contract. | Device performance, false-positive, Arabic/English, and battery tests. |
| Device-owner, accessibility, screen capture, background monitoring, SMS | These capabilities require explicit user consent, Android-specific configuration, and Play policy review; some are unavailable or restricted on iOS. | Validate each capability against target Android versions and distribution policy, then complete consent/onboarding on a physical device. | Only allowed APIs are enabled with transparent user indication. | Permission ladder, capability registry, and compliant abstractions. | Device and policy validation; no capability is claimed live before this evidence exists. |
| Phase 13 policy manager runtime validation | Policy configuration, SQLite/Outbox lifecycle, Firestore authorization, and Firestore rules/index deployment are evidenced. There is still no APK, AVD, or physical Android/iPhone runtime to exercise the `SafetyPoliciesScreen` end to end. | Build on a sufficiently provisioned Android host, then run with the Emulator flags in row 11. Create, edit, disable, override, restart offline, reconnect, and read back a policy. | Redacted runtime evidence confirms the policy UI is usable and the outbox-to-Firestore recovery behavior is correct. | 34 Flutter tests (including the policy Widget), 9 Firestore rules tests, 2 Functions Emulator tests, and Firestore rules/index deployment to `manus-guardian`. | Flutter runtime, offline/restart, network recovery, and real-client Firestore read/write evidence. |
| Phase 14 Android Usage Stats and enforcement validation | The public Usage Stats bridge source and unsupported enforcement adapter are implemented, but `:app:compileDebugKotlin` stopped at `:app:compileFlutterBuildDebug`/kernel snapshot under sandbox resource constraints. No APK, AVD, or physical Android device is available. | On a suitable Android host, build debug APK, then explicitly deny/grant Usage Access and test observation, policy persistence, offline/recovery, Doze, and reboot. Do not enable Accessibility or claim app blocking in this test. | Redacted device model/API/build hash and independent results for permission-denied, permission-granted, observed/no-observation, offline/recovery, restart, Doze, and reboot. | 42 Flutter tests, 10 Firestore Rules Emulator tests, 2 Functions Emulator tests; no Android Kotlin/device evidence. | Kotlin build result, APK install, native bridge execution, and physical-device lifecycle evidence. |
| Phase 15 physical Usage Access and APK gate | Screen-time policy, cumulative daily accounting, Kotlin UsageStats bridge source, truthful adapter semantics, and Emulator authorization are implemented. The debug APK attempt again stopped at `compileFlutterBuildDebug`/`kernel_snapshot_program` before Kotlin compilation under this sandbox. | Use the exact Phase 15 human-action build/device procedure; run deny/grant, target-package observation, daily-limit boundary, force-stop/reopen, offline/reconnect, and revocation tests. | Redacted APK hash, device/API/OEM information, captured permission state, measured target package total, persisted local total, rule decision, and Android adapter outcome. | 45 Flutter tests, 11 Firestore Rules Emulator tests, and 2 Functions Emulator tests; no APK/device evidence. | APK artifact, Kotlin compile, physical Usage Access, actual observation, lifecycle, and any supported OS enforcement evidence. |
| Phase 16 child/parent dialogue runtime gate | Child exception domain, atomic parent review/override, local expiry, parent/child UI, Outbox contract, and Firestore Emulator authorization are implemented. The child screen still needs a real child-authenticated runtime host, and the sandbox APK attempt stopped at `compileFlutterBuildDebug`/`kernel_snapshot_program`. | Follow `PHASE_16_HUMAN_ACTION_REQUIRED.md` with distinct child/parent identities against the Emulator, then evaluate an owner-approved real backend path separately. | Redacted child/parent UID distinction, device binding, request/create/cancel/approve/deny results, device-scoped override read-back, expiry, offline/restart evidence, APK hash, and adapter output. | 57 Flutter tests, 12 Firestore Rules Emulator tests, 2 Functions Emulator tests; no APK/device/FCM/real-backend evidence. | APK/Kotlin build, child-auth UI route, physical session, lifecycle evidence, optional explicit real-backend rules deployment, and no FCM claim. |
| Phase 17 recovery and membership gate | The sandbox reset removed local-only `lib/firebase_options.dart`, `android/app/google-services.json`, and the Android SDK. The membership repository was restored, 51 locally compilable Flutter tests passed, and 15 Rules plus 2 Functions Emulator tests passed. The full suite and analysis remain blocked by the missing original FlutterFire options; the APK build stops because no Android SDK exists. Dashboard intentionally cannot infer an authenticated owner-to-member binding. | Restore only the original approved local Firebase artifacts, run full Flutter analysis/tests, then build on an Android SDK-equipped host. Implement and test a fail-closed authenticated-account-to-local-member binding before enabling Dashboard owner actions. | Full Flutter suite/analysis passes, debug APK exists, device/Emulator run proves offline/expiry/cancel/revoke recovery, and actor binding rejects UID/family/member mismatch. | Local membership, contract, Widget, and Emulator evidence only; no real backend deployment or physical-device evidence. | Android/iPhone runtime, Flutter client ↔ Emulator, explicit real-backend approval, and reviewed deployment. |
| Phase 17 actor-binding closure | A fail-closed authenticated Firebase account → local FamilyMember resolver is implemented. It reads only the UID-keyed remote membership path, verifies remote/local family ID, member ID, UID, role, and active status, rejects child identities, and gates Dashboard actions by centralized permissions. Ten binding regression tests and a closure Emulator rerun passed. | Restore the two original approved local Firebase artifacts from secure canonical storage; then run full Flutter compile/tests and client-to-Emulator identity scenarios on an Android host. | Full Flutter suite/analysis passes; Android debug APK exists; Emulator-client proof covers valid parent/co-parent plus unknown, revoked, child, family mismatch, and remote-read failure. | No artifact was available in this sandbox; the separately supplied Google Services upload was not used. No deployment or production configuration change occurred. | Phase 17 closure remediation only; do not begin Phase 18, deploy rules, enable Blaze, or add FCM. |

> No blocker justifies a stack substitution or a fabricated implementation claim. The project continues with code and documentation that are independently reviewable, while every runtime-dependent item remains classified as **BLOCKED** or **HUMAN ACTION REQUIRED** until evidence is recorded.
