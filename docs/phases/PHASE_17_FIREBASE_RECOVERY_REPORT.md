# PHASE 17 — FIREBASE CONFIGURATION RECOVERY REPORT

**التاريخ:** 13 أغسطس 2026 (بتوقيت GMT+3)
**المشروع:** Guardian Eye Pro — Phase 17 Closure / Trusted Actor Binding
**الهدف:** استعادة تهيئة Firebase الرسمية + التحقق من صحة نقطة التفتيش Phase 17 الموجودة
**الإصدار:** تقرير استعادة تهيئة Firebase فقط — لا يُعتمد كحالة خضراء لمرحلة Phase 17

---

## 1. نتيجة مصادقة Firebase (Firebase Authentication Result)

تم تثبيت Firebase CLI بنسخة **15.26.0** في بيئة العمل الجديدة، وكانت الجلسة غير مسجَّلة الدخول كما هو متوقع (لأن الجلسة السابقة توقفت قبل استعادة التهيئة). بعد فتح تدفق `firebase login` في المتصفح وتسليم التحكم للمستخدم لإكمال تسجيل الدخول عبر Google، نجحت المصادقة:

| العنصر | النتيجة |
|---|---|
| Firebase CLI version | 15.26.0 |
| الحالة النهائية | ✔ Success — Logged in as 24160037@su.edu.ye |
| الحساب المستخدم | نفس الحساب المرتبط بمشروع manus-guardian |

> ملاحظة: تم الالتزام الصارم بقاعدة عدم استلام أي بيانات اعتماد داخل المحادثة — اكتمل تسجيل الدخول حصرياً عبر تدفق متصفح Google/CLI.

## 2. التحقق من مشروع Firebase (Firebase Project Verification)

نتيجة `firebase projects:list --json` أكدت وصول الحساب إلى المشروع المطلوب وتطابق الهوية بالكامل:

| العنصر | القيمة المتوقعة | القيمة الفعلية | الحالة |
|---|---|---|---|
| Project ID | manus-guardian | manus-guardian | ✔ مطابق |
| Project Name (displayName) | Manus-guardian-eye | Manus-guardian-eye | ✔ مطابق |
| Project Number | 165160049292 | 165160049292 | ✔ مطابق |
| State | ACTIVE | ACTIVE | ✔ نشط |

لم يتم إنشاء أي مشروع جديد، ولم يتم تعديل أي مورد إنتاجي.

## 3. التحقق من تطبيق Android (Android Application Verification)

نتيجة `firebase apps:list ANDROID --project manus-guardian`:

| العنصر | القيمة المتوقعة | القيمة الفعلية | الحالة |
|---|---|---|---|
| App Display Name | guardianeye | guardianeye | ✔ موجود |
| App ID | 1:165160049292:android:922e6c8a4749c42e4839a9 | 1:165160049292:android:922e6c8a4749c42e4839a9 | ✔ مطابق تماماً |
| عدد تطبيقات Android | تطبيق واحد | تطبيق واحد | ✔ لا يوجد تطبيق إضافي |

التطبيق الوحيد المسجل هو تطبيق Android الأصلي، ولم يتم تسجيل أي تطبيق iOS أو Windows أو Web، ولم يُنشأ أي تطبيق جديد.

## 4. إصدار FlutterFire (FlutterFire Version)

| الأداة | الإصدار |
|---|---|
| FlutterFire CLI | 1.4.1 (dart pub global activate) |
| Flutter | 3.35.5 (stable) |
| Dart | 3.9.2 |

## 5. استعادة firebase_options.dart

تم توليد الملف رسمياً عبر FlutterFire CLI (`flutterfire configure --project=manus-guardian --platforms=android --yes`) دون أي كتابة يدوية أو ملفات placeholder:

| عنصر التحقق | القيمة في الملف | الحالة |
|---|---|---|
| projectId | manus-guardian | ✔ |
| appId | 1:165160049292:android:922e6c8a4749c42e4839a9 | ✔ |
| apiKey / messagingSenderId | الرسمية من Firebase | ✔ |
| ترويسة الملف | "File generated by FlutterFire CLI" | ✔ ملف رسمي |

**التوافق مع GuardianFirebaseBootstrap:** الملف يستورد `DefaultFirebaseOptions.currentPlatform` ويتوافق مع `Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform)` الموجود في `lib/core/firebase/guardian_firebase_bootstrap.dart` دون أي تعديل على منطق التمهيد.

## 6. استعادة google-services.json

تم توليد الملف رسمياً في `android/app/google-services.json`:

| عنصر التحقق | القيمة في الملف | الحالة |
|---|---|---|
| project_id | manus-guardian | ✔ |
| project_number | 165160049292 | ✔ |
| package_name | com.guardianeye.app | ✔ مطابق لحزمة التطبيق |
| mobilesdk_app_id | 1:165160049292:android:922e6c8a4749c42e4839a9 | ✔ |
| configuration_version | 1 | ✔ |

يتطابق `package_name` تماماً مع `applicationId = "com.guardianeye.app"` في `android/app/build.gradle.kts`.

## 7. تغييرات firebase.json

بمقارنة `firebase.json` الأصلي (من الأرشيف المرفوع) مع الحالي:

- **البنية الأساسية (functions, firestore, storage, emulators):** لم تتغير — مشروع guardian codebase، قواعد firestore.rules، محاكيات auth:9099/firestore:8080/functions:5001، وضع singleProjectMode كما هو.
- **قسم flutter:** تم تحديثه تلقائياً من FlutterFire CLI لتسجيل المسارات الجديدة:
  - android default: projectId manus-guardian / appId الرسمي / output إلى android/app/google-services.json
  - dart: lib/firebase_options.dart مع projectId manus-guardian وتهيئة android

هذا تحديث تشغيلي متوقع من FlutterFire CLI فقط، ولا يمس قواعد الإنتاج ولا نشر أي خدمة.

## 8. الملفات المتغيرة (Files Changed)

| الملف | نوع التغيير | ملاحظة |
|---|---|---|
| `lib/firebase_options.dart` | **جديد** (استعادة) | مولَّد رسمياً من FlutterFire |
| `android/app/google-services.json` | **جديد** (استعادة) | مولَّد رسمياً من FlutterFire |
| `firebase.json` | **معدَّل** (قسم flutter فقط) | تحديث تشغيلي من FlutterFire CLI |
| `pubspec.lock` / `.dart_tool/` | تعديل تشغيلي | نتيجة `flutter pub get` (39 تبعية محدثة مع Dart 3.9.2) |
| `nohup.out` | مؤقت | خرج جلسة تسجيل الدخول |

## 9. الملفات التي لم تُمس عمداً (Files Intentionally Untouched)

جميع ملفات منطق المرحلة 17 بقيت كما هي دون أي تعديل، ومنها على سبيل المثال لا الحصر:

- `lib/core/firebase/guardian_firebase_bootstrap.dart` و`guardian_firebase_environment.dart`
- ملفات Family Membership وTrusted Actor Binding (domain/application/data)
- `lib/core/localization/*` و`assets/translations/*`
- `firebase/firestore.rules` و`firebase/storage.rules` و`firebase/functions/*`
- `firebase/tests/*` (اختبارات المحاكي غير معدلة)
- `tool/run_firebase_emulator_tests.sh` و`pubspec.yaml` و`.firebaserc` و`AndroidManifest.xml` و`build.gradle.kts`
- لا يوجد أي Admin SDK credentials أو service-account private keys في المشروع

## 10. نتيجة flutter pub get

نجح تنفيذ `flutter pub get` بالكامل: تم تحديث **39 تبعية** بنجاح، مع تنبيهات اعتيادية بوجود إصدارات أحدث غير متوافقة مع قيود الأصدار الحالية (78 حزمة) — لا تمثل فشلاً.

## 11. نتيجة flutter analyze

```
Analyzing guardian_eye...
No issues found! (ran in 19.4s)
```

**0 issues** — مطابق للحالة المتوقعة، بعد تثبيت `libsqlite3-dev` المطلوب من sqflite_common_ffi في بيئة Linux.

## 12. نتيجة مجموعة اختبارات Flutter الكاملة

```
68 PASS — 5 FAIL
```

تم إعادة التشغيل بعد تثبيت مكتبة sqlite3 الأصلية (كان التشغيل الأول يفشل عالمياً بسبب غياب libsqlite3.so، وهذا عامل بيئة وليس خللاً في المشروع).

## 13. نتيجة اختبارات Actor Binding

```
test/family_actor_binding_service_test.dart: 10 PASS (10/10) — All tests passed
```

تشمل: valid parent/co-parent binding، الفشل الآمن عند UID غير معروف، رفض العضو غير النشط، رفض العضو الملغى، منع وثائق العائلات الأخرى، منع هوية الطفل من التصرف كبالغ، تطابق الأدوار، والفشل الآمن عند عدم المصادقة — مطابقة للحالة المتوقعة.

## 14. نتيجة اختبارات Membership

```
test/family_membership_test.dart: 4 PASS (4/4) — All tests passed
```

## 15. نتيجة محاكي قواعد Firestore (Firestore Emulator)

```
firebase/tests: 15 PASS — 0 FAIL
```

تشمل: عزل العائلات، منع تصعيد أدوار الأطفال، نظام الدعوات الذري، إدارة السياسات، ربط أجهزة الأطفال عبر provisioning فقط، تسجيل الرموز، حوادث الأجهزة، طلبات الاستثناءات، ومنع كتابة أحداث الإشعارات مباشرة من العميل المتنقل — مطابقة للحالة المتوقعة (15 PASS).

## 16. نتيجة محاكي الدوال (Functions Emulator)

```
firebase/functions: 2 PASS — 0 FAIL
```

تشمل: حوادث incident/SOS تنتج أحداث إشعارات دائمة دون الادعاء بتسليم FCM، وربط provisioning لجهاز الطفل لUID مميز مرة واحدة مع رفض إعادة التشغيل — مطابقة للحالة المتوقعة (2 PASS).

## 17. حالة Android SDK

| العنصر | الحالة |
|---|---|
| Android SDK | **غير مثبت** في بيئة العمل (ANDROID_HOME غير محدد، لا يوجد /usr/lib/android-sdk) |
| تصنيف التحقق من APK | **HUMAN ACTION REQUIRED** |

وفقاً لتعليمات المهمة: لم يتم تثبيت أو إعادة بناء Android SDK، ولم يتم تغيير كود المشروع لإخفاء هذا القيد. بناء APK والتحقق منه يتطلب خطوة يدوية في بيئة تحوي Android SDK.

## 18. النتيجة الأمنية (Security Result)

| البند | النتيجة |
|---|---|
| عدم إنشاء مشروع Firebase | ✔ لم يُنشأ أي مشروع |
| عدم إنشاء تطبيق Android إضافي | ✔ التطبيق الوحيد هو التطبيق الأصلي |
| عدم تسجيل تطبيق iOS | ✔ لا توجد تطبيقات iOS |
| عدم تفعيل Blaze | ✔ لم تُلمس خطة الفوترة |
| عدم نشر Firestore Rules / Functions / FCM | ✔ لم يُنفذ أي deploy |
| عدم تعديل بيانات الإنتاج | ✔ جميع الاختبارات على محاكيات معزولة (guardian-eye-emulator) |
| عدم إدخال Admin SDK أو مفاتيح حساب خدمة | ✔ لا توجد أي credentials داخل المشروع |
| عدم تعديل منطق Phase 17 | ✔ لم يُعدل أي ملف domain/application/data |
| عدم تعديل Family Membership / Trusted Actor Binding / Dashboard Authorization | ✔ |
| عدم بدء Phase 18 | ✔ |
| حساب المصادقة | مطابق للحساب الأصلي للمشروع |

## 19. العوائق المتبقية (Remaining Blockers)

**1) PHASE 17 LOCALIZATION REMEDIATION REQUIRED** — أربعة إخفاقات ترجمة موجودة مسبقاً (pre-existing) في `test/exception_request_screen_test.dart`:

| الاختبار الفاشل | الملاحظة |
|---|---|
| Arabic parent review renders truthful request and queued states | لم يُجد النص "بانتظار مراجعة الوالد" |
| Arabic review renders an expired request state | لم يُجد النص "انتهت صلاحية الطلب" |
| English parent review renders the same request states | نص إنجليزي معروض مختلف عن المتوقع |
| Arabic child policy experience explains offline local facts | نص عربي غير مطابق |

وفقاً للتعليمات: **لم يتم إصلاحها في هذه المهمة** وتُبلغ بشكل منفصل كـ PHASE 17 LOCALIZATION REMEDIATION REQUIRED.

**2) إخفاق منطقي واحد غير متعلق بالتهيئة الجديدة** — `test/local_repository_test.dart: stored policy and override remain evaluable offline` يفشل بـ `Override must target an item and expire in the future` في `PolicyRepository.createOverride` عند سطر 53 في الاختبار. هذا فشل في بيانات الاختبار (fixture) وليس نتيجة استعادة Firebase، ويُوثق هنا للشفافية فقط دون إصلاح.

**3) Android APK validation — HUMAN ACTION REQUIRED** بسبب غياب Android SDK في بيئة العمل (انظر البند 17).

**4) إجمالي suite Flutter المتوقع** — النتيجة الفعلية (68 PASS) أقل من الرقم المتوقع سابقاً (~69 PASS) بسبب الإخفاقين الإضافيين الموثقين في البندين 19.1 و19.2 أعلاه.

---

## الخلاصة

تمت **استعادة تهيئة Firebase الرسمية بنجاح** للملفين `lib/firebase_options.dart` و`android/app/google-services.json` عبر FlutterFire CLI 1.4.1 الرسمي، مع تطابق كامل للهوية (manus-guardian / com.guardianeye.app / 1:165160049292:android:922e6c8a4749c42e4839a9) وعدم وجود أي علامات placeholder. تم التحقق من نقطة التفتيش Phase 17: تحليل نظيف (0 issues)، اختبارات Actor Binding (10 PASS)، Membership (PASS)، Firebase Contract (PASS)، Family Members UI (PASS)، Firestore Emulator (15 PASS)، Functions Emulator (2 PASS)، مع الالتزام الكامل بقواعد الأمان وعدم تعديل أي منطق إنتاجي.

**لا يتم اعتماد Phase 17 كحالة خضراء (GREEN).** هدف المهمة كان فقط: استعادة تهيئة Firebase الرسمية + التحقق من صحة نقطة التفتيش Phase 17 — وقد اكتمل ذلك، مع الإبلاغ المنفصل عن متطلبات معالجة الترجمة.
