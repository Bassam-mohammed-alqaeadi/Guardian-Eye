# تقرير المرحلة 0 — حماية العمل وتسجيل Baseline

**التاريخ:** 20 أغسطس 2026 | **النطاق:** بروتوكول إصلاح الفجوات — المرحلة 0 فقط | **الأدوار المنفذة:** مدير تنفيذ + مراجع جودة

## 1. اسم المرحلة

المرحلة 0 — حماية العمل والتحقق الأولي (Protection & Baseline).

## 2. الهدف

تثبيت نقطة انطلاق آمنة وموثقة قبل أي تعديل، والتأكد من أن عمل FS-007 وFS-008 محفوظ، وتسجيل نتائج Baseline لكل أدوات الجودة لتكون مرجع المقارنة لجميع المراحل اللاحقة (1–10).

## 3. المشكلة والدليل

المشكلة الموثقة في تقرير الاستكشاف (`docs/00_master/PROJECT_DISCOVERY_HANDOFF_REPORT.md`): قبل بدء أي إصلاح يجب التأكد من عدم وجود عمل غير محفوظ، لأن فقدان عمل FS-007/FS-008 كان سيجعل أي إصلاح لاحق فوق أساس غير مستقر. **الدليل:** فحص `git status` أظهر شجرة عمل نظيفة (0 ملفات غير محفوظة) والتزام `86f4cbf` مرفوعاً ومتطابقاً مع origin.

## 4. الحالة قبل التنفيذ

| البند | الحالة الفعلية (مفحوصة من الكود) |
|---|---|
| الفرع الحالي | `feature/design-system-integration` |
| آخر التزام | `86f4cbf feat(fs007-fs008): stabilize tasks and rewards` |
| التطابق مع origin | **مطابق تماماً** (`86f4cbf199a249a7c40838a2065ae0bfc66ea1e1` محلياً وبُعدياً) |
| ملفات غير محفوظة | **0** |
| FS-007/FS-008 محفوظة في Git | **نعم** — `git log -1 -- lib/domain/family_tasks.dart` يعود بـ `86f4cbf` |
| نقطة تحقق (Checkpoint) | علامة Git أنشئت: `phase0-baseline-86f4cbf` (دفعها إلى origin) |

## 5. خطة الإصلاح

لا إصلاح في هذه المرحلة — فحص وتحقق فقط، دون تعديل أي ملف. الخطة: (1) فحص الفرع والحالة، (2) إنشاء checkpoint tag، (3) مسح أسرار، (4) format للتحقق، (5) analyze، (6) انحدار كامل، (7) تسجيل Baseline.

## 6. الملفات المعدلة

**لا شيء.** المرحلة قراءة وفحص فقط.

## 7. الملفات التي لم تُعدَّل

جميع ملفات المستودع دون استثناء.

## 8. الحزم الجديدة

لا توجد — لم تُضف أي حزمة.

## 9. التغييرات المنفذة

إنشاء علامة Git موثقة `phase0-baseline-86f4cbf` مرفوعة إلى origin، يمكن استعادة أي مرحلة منها لاحقاً عبر `git checkout phase0-baseline-86f4cbf`.

## 10. الاختبارات المضافة

لا اختبارات جديدة في هذه المرحلة (فحص فقط). اختبارات المرحلة القادمة ستُوثق في تقارير المراحل 1–10.

## 11. أوامر التحقق التي تم تشغيلها

```bash
git branch --show-current                        # branch
git log --oneline -1                             # last commit
git status --short                               # uncommitted files
git rev-parse HEAD vs origin/feature/...         # remote parity
git tag -a phase0-baseline-86f4cbf && git push origin phase0-baseline-86f4cbf
grep -rnEi (secrets patterns) lib/ pubspec.yaml firebase.json
dart format --output=none --set-exit-if-changed lib/ test/
flutter analyze lib/ test/
flutter test $(ls test/*.dart | grep -v headless_validation | grep -v test_database)
```

## 12. نتائج format

`dart format --output=none --set-exit-if-changed` لم يعدّل أي ملف خارج الشجرة (output=none لا يكتب)، وشجرة العمل بقيت **0 ملفات متغيرة** بعده — التنسيق الحالي متطابق مع الالتزام `86f4cbf`. ملاحظة: تشغيل format بوضع الكتابة (كما في التثبيت السابق) يكشف أن بعض ملفات المشروع القديمة تحتاج تنسيقاً؛ هذا مرشح لمرحلة 10 وليس المرحلة 0، ولن يُنفذ دون موافقتك.

## 13. نتائج flutter analyze

| النتيجة | العدد |
|---|---|
| **أخطاء (error)** | **0** |
| **تحذيرات (warning)** | **0** |
| ملاحظات (info، سابقة الوجود، في ملفات غير متأثرة) | 423 |

## 14. نتائج flutter test

**380/380 — All tests passed.** لا فشل واحد، لا تعليق. السجل المحفوظ: `/tmp/ph0_regression.log`.

## 15. نتائج build

لم تُنفذ — غير مطلوبة في هذه المرحلة (فحص فقط). البناء متاح كخطوة عند أي مرحلة تطلب APK.

## 16. نتائج Android / Firebase Emulator / جهاز حقيقي

لم تُنفذ — لا يوجد جهاز Android أو Emulator في هذه البيئة (قيد موثق من جلسات سابقة، ولا تنزيل بيئات جديدة). هذا القيد ينطبق على المراحل التي تتطلب إثباتاً جهازياً (الإشعارات، الموقع في الخلفية، lifecycle) وسيقع ضمن قسم "الأشياء غير المتحقق منها" لتلك المراحل.

## 17. المخاطر المتبقية

| # | الخطر | الدرجة | الإجراء |
|---|---|---|---|
| 1 | apiKey في `lib/firebase_options.dart` مكشوف في الشجرة ([REDACTED] من الطباعة العامة، موجود في الكود) — هذا ملف مولّد من `flutterfire configure` ومقبول معماریاً، لكنه يعني أن مفاتيح المشروع العامة متاحة لأي من يسحب الفرع | متوسطة | غير قابل للإصلاح ضمن نطاق هذا البروتوكول دون موافقة (إعادة تدوير المفاتيح قرار إنتاجي) |
| 2 | قاعدة Firestore المنشورة غير مثبتة مطابقة للملف المحلي (`firestore.rules`) | متوسطة | بند المرحلة 1 |
| 3 | الخادم Render والـ FCM يحتاجان تحقق صحة حي | متوسطة | بند المرحلة 3 |
| 4 | لا تحقق Android حقيقي متاح في هذه البيئة | مرتفعة لمرحلة 2 و3 | يُوثق كبند "غير متحقق منه" دائماً |

## 18. الأشياء غير المتحقق منها

اختبار Android حقيقي (غير ممكن بيئياً)، تطابق قواعد Firestore المنشورة مع الملف المحلي (بند المرحلة 1)، سلامة خادم Render الحي (بند المرحلة 3)، عمل FCM end-to-end (بند المرحلة 2).

## 19. commit hash

لا commit جديد — الشجرة نظيفة. نقطة التحقق: **tag `phase0-baseline-86f4cbf`** على الالتزام `86f4cbf` (مرفوع).

## 20. القرار

**READY** لبدء المرحلة 1 (Firestore Rules والصلاحيات والأمان)، وفق الترتيب الإلزامي للبروتوكول. لا يوجد خطر حرج غير محلول يمنع الانتقال، لأن العمل محفوظ ومفحوص والاختبارات خضراء.

## 21. السؤال أو الموافقة المطلوبة

هل توافق على بدء **المرحلة 1 — Firestore Rules والصلاحيات والأمان** كما هي محددة في البروتوكول (فحص firebase.json / firestore.rules / family authorization / اختبارات السماح والرفض الإثني عشر دون نشر إنتاجي)، أم تريد تعديل نطاقها أو البدء بمرحلة أخرى؟
