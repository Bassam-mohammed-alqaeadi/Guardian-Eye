# Guardian Eye Pro — Reconciled Current-Source Gap Audit

**Audit point:** after pairing, policy, incident, SOS, retry, and authorization additions currently present in the Flutter source tree. This document supersedes `GAP_AUDIT_PHASE_5.md` where classifications differ.

| Requirement | Current implementation location | Executable locally | Tests | Verified evidence | Environment dependency | Final classification |
|---|---|---:|---:|---|---|---|
| Family creation, primary-parent record, child creation, and local outbox | `FamilyRepository` in `lib/data/guardian_repositories.dart` | Yes, SQLite runtime required | Validation/domain only; repository test pending | UI/widget and domain suite, not repository persistence | Device SQLite runtime for full repository proof | IMPLEMENTED + NOT YET FULLY VERIFIED |
| Member roles and family/device authorization | `family_authorization.dart`; role models | Yes | `family_authorization_test.dart` | 3 focused tests passed | None for domain; Firebase needed for server enforcement | IMPLEMENTED + VERIFIED (domain only) |
| Pairing request, hashing, expiry, five-failure lockout, device enrollment, ownership, duplicate prevention, revoke | `PairingRepository`, schema v3, `PairingLifecycle` | Yes, SQLite runtime required | Hash/lifecycle test only | Domain transitions passed; repository transitions not yet exercised | Device SQLite runtime for repository proof; Firebase for remote verification | PARTIALLY IMPLEMENTED |
| Device remote state/health and child-side verification | Device table/schema only | No complete flow | No | None | Firebase, child device, physical Android/iOS | ARCHITECTURE ONLY |
| Active policy, priority, conflict, temporary override, expiration, offline evaluation | `policy_engine.dart` | Yes | `policy_incident_sos_test.dart` | Precedence and active override passed | None for evaluator | IMPLEMENTED + VERIFIED (domain only) |
| Policy persistence, policy outbox, sync event, policy configuration UI | Schema only | No end-to-end path | No | None | Firebase for remote sync | NOT IMPLEMENTED |
| Model abstraction / unavailable state | `incident_engine.dart` `SafetyModelAdapter` and `UnconfiguredSafetyModelAdapter` | Yes | Indirect lifecycle/risk tests | Model-unavailable implementation is explicit | Actual model artifact required | IMPLEMENTED + VERIFIED (abstraction); AI inference BLOCKED |
| Risk assessment, incident creation/ack lifecycle, local incident outbox | `RiskEngine`, `IncidentLifecycle`, `IncidentRepository` | Yes, SQLite runtime required | Risk and lifecycle tests | Domain tests passed; repository persistence pending | Device SQLite runtime; Firebase for sync | PARTIALLY IMPLEMENTED |
| Incident notification | No producer/receiver contract yet | No | No | None | Firebase/FCM/APNs and physical device | NOT IMPLEMENTED |
| SOS local created/pending/outbox/transitions | `SosRepository`, `SosLifecycle`, SOS screen | Yes, SQLite runtime required | Lifecycle test only | Domain transition test passed; repository persistence pending | Location/device runtime for location source | PARTIALLY IMPLEMENTED |
| SOS sync, notified, acknowledgement, SMS fallback | State contract only | No end-to-end delivery | No | No delivery evidence | Firebase, FCM, carrier/SMS policy, physical device | ARCHITECTURE ONLY / HUMAN ACTION REQUIRED |
| Outbox retry / recovery behavior | `OutboxRetryPolicy`, outbox schema | Yes | `outbox_retry_test.dart` | Backoff and max-attempt tests passed | Background worker/device required for process/Doze validation | PARTIALLY IMPLEMENTED |
| Outbox idempotency, duplicate prevention, failure classification, conflict resolution | DB uniqueness field and operation contract only | No full executor | No repository test | None | Firebase emulator and device/background execution | ARCHITECTURE ONLY |
| Firebase Auth / user context / Firestore repo | Guarded checks in `sync_services.dart`, rule templates | No configured runtime | No | No Firebase project configured | Firebase project and FlutterFire config | HUMAN ACTION REQUIRED |
| Firestore family/device/incident/SOS security | `firebase/firestore.rules` / `storage.rules` templates | Not deployed | No emulator tests | Rules are static templates only | Firebase emulator/project | ARCHITECTURE ONLY / HUMAN ACTION REQUIRED |
| Notification architecture | Dependency only; no event or token contract | No | No | None | FCM/APNs credentials and physical device | NOT IMPLEMENTED |
| Android permissions/settings bridge | Android manifest and `MainActivity.kt`; capability gateway | Code exists | No native test | Not built/installed on a device | Android SDK/NDK/device and policy review | IMPLEMENTED + NOT YET VERIFIED |
| iPhone host and privacy strings | `ios/Runner/Info.plist` and host project | Not on Linux | No | Not built | macOS/Xcode/CocoaPods/signing/iPhone | BLOCKED BY ENVIRONMENT |

> **Reconciliation decision:** The current policy evaluator is no longer classified as “not implemented”; it is **implemented and domain-tested**, but its persistence and synchronization slices remain unimplemented. The incident/safety lifecycle is no longer “architecture only”; the domain risk and transition engines plus local repository contract are implemented, while persistence, notification, and Firebase delivery require additional tests and environment setup.
