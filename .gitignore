# Tool and build outputs
.dart_tool/
build/
.gradle/
android/.gradle/
android/local.properties
ios/Flutter/ephemeral/

# Firebase platform configuration is committed with every push (project owner policy).
# firebase_options.dart, google-services.json, GoogleService-Info.plist are included in Git.
