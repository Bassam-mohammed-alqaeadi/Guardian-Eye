<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required on Android 15+ (API 35+) when a foreground service uses the
         dataSync type; declared for the M8 monitoring service (M8C closure). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Location permissions for real-time background location tracking
         (foreground service of Android type `location`). -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- ACCESS_BACKGROUND_LOCATION is a runtime grant on Android 10+; the app
         asks for it through the permission flow before starting tracking. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Required on Android 14+ (API 34+) when a foreground service uses the
         `location` type; without it the OS rejects startForeground entirely. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
<application
        android:label="Guardian Eye Pro"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Background location tracking: transparent foreground service of
             Android type `location`. Captures fresh positions on a fixed
             interval so the family map and location history stay fresh while
             the app is in the background (boot resilience via BootReceiver).
             Android 14+ (API 34+): foregroundServiceType MUST match the
             workload; `location` is the legitimate type for position capture
             and requires the FOREGROUND_SERVICE_LOCATION permission declared
             above. -->
        <service
            android:name=".LocationTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />
        <!-- M8 screen-time enforcement: transparent foreground monitoring service.
             foregroundServiceType="dataSync" (M8C closure, 2026-08-16): the service's
             real workload is collecting usage observations that feed the family
             policy/outbox sync pipeline, which maps to the legitimate dataSync type
             required by Android 14+ (API 34+). See
             docs/UX_SPRINT_01_M8_ENFORCEMENT_DECISION.md path A. -->
        <service
            android:name=".EnforcementService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <service
            android:name=".AudioCaptureService"
            android:exported="false"
            android:foregroundServiceType="microphone" />
        <!-- M8 boot resilience: restart enforcement monitoring after reboot.
             WorkManager periodic rescheduling after boot is handled by the
             workmanager Flutter plugin. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
