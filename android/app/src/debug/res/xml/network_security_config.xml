<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only network security policy.
     Permits cleartext HTTP ONLY to local Firebase Emulator hosts so the app
     can talk to the Auth/Firestore/Functions emulator during development.
     This file lives under src/debug and is never packaged into release
     builds, so production traffic stays HTTPS-only. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <!-- Host loopback from the Android Emulator to the dev machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
